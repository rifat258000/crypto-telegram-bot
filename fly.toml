app = "crypto-tg-bot-rifat"
primary_region = "iad"

[build]

[env]

[processes]
  app = "python -m bot.main"

[restart]
  policy = "always"
  max_retries = 100

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  cpus = 1
